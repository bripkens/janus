        <hudson.tasks.Maven>
            <targets>${encode(job['targets'])}</targets>
            <mavenName>(Default)</mavenName>
            <pom>${encode(job['pom'])}</pom>
            <usePrivateRepository>${job['usePrivateRepository']}</usePrivateRepository><% if (job['jvmOptions']) { %>
            <jvmOptions>${encode(job['jvmOptions'])}</jvmOptions><% } %>
        </hudson.tasks.Maven>